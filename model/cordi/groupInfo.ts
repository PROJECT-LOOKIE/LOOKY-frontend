export type GroupInfoDTO = {
  id: number;
  emoji: string;
  name: string;
  location: string;
  atmosphere: string;
  userName: string;
  people: number;
};

export type Mode = {
  mode: string;
};
